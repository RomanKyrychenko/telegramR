# Get Input Group Call

Similar to `get_input_peer`, but for input calls.

## Usage

``` r
get_input_group_call(call)
```

## Arguments

- call:

  The call object to convert.

## Value

An InputGroupCall object.
