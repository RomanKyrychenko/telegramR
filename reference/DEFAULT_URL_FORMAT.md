# Default URL format used by unparse (deprecated in some flows).

Default URL format used by unparse (deprecated in some flows).

## Usage

``` r
DEFAULT_URL_FORMAT
```

## Format

An object of class `character` of length 1.
