# Unpack a 64-bit integer from a raw vector (little-endian)

Unpack a 64-bit integer from a raw vector (little-endian)

## Usage

``` r
unpackInt64(raw_vector)
```

## Arguments

- raw_vector:

  A raw vector containing the 64-bit integer

## Value

The unpacked 64-bit integer as a numeric value
