# Various helpers not related to the Telegram API itself Enum for entity types

Various helpers not related to the Telegram API itself Enum for entity
types

## Usage

``` r
EntityType
```

## Format

An object of class `list` of length 3.
