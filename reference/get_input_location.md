# Get Input Location

Similar to `get_input_peer`, but for input messages.

## Usage

``` r
get_input_location(location)
```

## Arguments

- location:

  The location object.

## Value

A list with elements `dc_id` and `location`.

## Details

Note that this returns a list `(dc_id, location)`, the `dc_id` being
present if known.
