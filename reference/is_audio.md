# Check if File is Audio

Returns \`TRUE\` if the file has an audio mime type, either from
metadata or guessed from the file extension.

## Usage

``` r
is_audio(file)
```

## Arguments

- file:

  The file path or object to check.

## Value

A logical value indicating whether the file is audio.
