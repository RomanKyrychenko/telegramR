# EncryptedMessageService

Telegram API type EncryptedMessageService

## Public fields

- `chat_id`:

  Field.

- `date`:

  Field.

- `bytes`:

  Field.

- `random_id`:

  Field.

## Methods

### Public methods

- [`EncryptedMessageService$new()`](#method-EncryptedMessageService-new)

- [`EncryptedMessageService$to_list()`](#method-EncryptedMessageService-to_list)

- [`EncryptedMessageService$clone()`](#method-EncryptedMessageService-clone)

------------------------------------------------------------------------

### Method `new()`

#### Usage

    EncryptedMessageService$new(chat_id, date = NULL, bytes, random_id = NULL)

------------------------------------------------------------------------

### Method `to_list()`

#### Usage

    EncryptedMessageService$to_list()

------------------------------------------------------------------------

### Method `clone()`

The objects of this class are cloneable with this method.

#### Usage

    EncryptedMessageService$clone(deep = FALSE)

#### Arguments

- `deep`:

  Whether to make a deep clone.
