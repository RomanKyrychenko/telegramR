# Register some of the most common mime-types to avoid any issues.

Register some of the most common mime-types to avoid any issues.

Extend the mimetypes character vector with additional common types.

## Usage

``` r
mimetypes

mimetypes
```

## Format

An object of class `character` of length 1565.

An object of class `character` of length 1565.
