# EncryptedMessage

Telegram API type EncryptedMessage

## Public fields

- `chat_id`:

  Field.

- `date`:

  Field.

- `bytes`:

  Field.

- `file`:

  Field.

- `random_id`:

  Field.

## Methods

### Public methods

- [`EncryptedMessage$new()`](#method-EncryptedMessage-new)

- [`EncryptedMessage$to_list()`](#method-EncryptedMessage-to_list)

- [`EncryptedMessage$clone()`](#method-EncryptedMessage-clone)

------------------------------------------------------------------------

### Method `new()`

#### Usage

    EncryptedMessage$new(chat_id, date = NULL, bytes, file, random_id = NULL)

------------------------------------------------------------------------

### Method `to_list()`

#### Usage

    EncryptedMessage$to_list()

------------------------------------------------------------------------

### Method `clone()`

The objects of this class are cloneable with this method.

#### Usage

    EncryptedMessage$clone(deep = FALSE)

#### Arguments

- `deep`:

  Whether to make a deep clone.
