# Adds a new public key to the server keys.

Adds a new public key to the server keys.

## Usage

``` r
add_key(pub, old = FALSE)
```

## Arguments

- pub:

  The public key in PEM format.

- old:

  Logical indicating if the key is old.
