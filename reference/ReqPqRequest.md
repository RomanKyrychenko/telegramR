# ReqPqRequest R6 class

Methods: - new(nonce): create new instance. - to_list(): return an R
list representation. - to_raw(): serialize to raw vector (bytes) in
little endian as used in TL.

Note: 128-bit integer handling is represented here as numeric/double
placeholders. For precise 128-bit two's-complement behavior, use a
big-integer library and implement a precise serializer.

Represents a request for the server's PQ (prime factor) during the
initial key exchange. Holds a 128-bit \`nonce\` generated by the client.

## Details

Represents the TL request \`ReqPqRequest\`.

## Public fields

- `nonce`:

  Field. Initialize a ReqPqRequest

- `class`:

  Field.

## Active bindings

- `class`:

  Field.

## Methods

### Public methods

- [`ReqPqRequest$new()`](#method-ReqPqRequest-new)

- [`ReqPqRequest$to_list()`](#method-ReqPqRequest-to_list)

- [`ReqPqRequest$to_bytes()`](#method-ReqPqRequest-to_bytes)

- [`ReqPqRequest$clone()`](#method-ReqPqRequest-clone)

------------------------------------------------------------------------

### Method `new()`

#### Usage

    ReqPqRequest$new(nonce)

------------------------------------------------------------------------

### Method `to_list()`

#### Usage

    ReqPqRequest$to_list()

------------------------------------------------------------------------

### Method `to_bytes()`

#### Usage

    ReqPqRequest$to_bytes()

------------------------------------------------------------------------

### Method `clone()`

The objects of this class are cloneable with this method.

#### Usage

    ReqPqRequest$clone(deep = FALSE)

#### Arguments

- `deep`:

  Whether to make a deep clone.

## Public fields

- `CONSTRUCTOR_ID`:

  Constructor identifier for this TL object.

- `SUBCLASS_OF_ID`:

  Subclass identifier for this TL object.

- `nonce`:

  Field. Initialize a new ReqPqRequest

- `class`:

  Field.

## Active bindings

- `class`:

  Field.

## Methods

### Public methods

- [`ReqPqRequest$new()`](#method-ReqPqRequest-new)

- [`ReqPqRequest$to_list()`](#method-ReqPqRequest-to_list)

- [`ReqPqRequest$to_bytes()`](#method-ReqPqRequest-to_bytes)

- [`ReqPqRequest$clone()`](#method-ReqPqRequest-clone)

------------------------------------------------------------------------

### Method `new()`

#### Usage

    ReqPqRequest$new(nonce)

#### Arguments

- `nonce`:

  128-bit nonce generated by the client (large int or raw). Convert to
  list representation

  Returns a plain R list representation useful for debugging or
  JSON-like inspection. The list includes the class discriminator and
  the \`nonce\`.

------------------------------------------------------------------------

### Method `to_list()`

#### Usage

    ReqPqRequest$to_list()

#### Returns

Named list with keys \`"\_\`" and \`nonce\`. Serialize to raw bytes

Serializes the request according to the protocol: - constructor id (4
bytes, little-endian) - \`nonce\` as a 128-bit (16-byte) little-endian
value

------------------------------------------------------------------------

### Method `to_bytes()`

#### Usage

    ReqPqRequest$to_bytes()

#### Returns

Raw vector containing the serialized request bytes.

------------------------------------------------------------------------

### Method `clone()`

The objects of this class are cloneable with this method.

#### Usage

    ReqPqRequest$clone(deep = FALSE)

#### Arguments

- `deep`:

  Whether to make a deep clone.
