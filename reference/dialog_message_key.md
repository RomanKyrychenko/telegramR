# Helper function to get dialog message key

Helper function to get dialog message key

## Usage

``` r
dialog_message_key(peer, message_id)
```

## Arguments

- peer:

  The peer object

- message_id:

  The message ID

## Value

A list containing the channel ID and message ID
