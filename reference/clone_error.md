# Clone an error object

Creates a copy of an error object preserving its class, message and
other attributes

## Usage

``` r
clone_error(error)
```

## Arguments

- error:

  The error object to clone

## Value

A new error object with the same properties
