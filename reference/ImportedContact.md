# ImportedContact

Telegram API type ImportedContact

## Public fields

- `user_id`:

  Field.

- `client_id`:

  Field.

## Methods

### Public methods

- [`ImportedContact$new()`](#method-ImportedContact-new)

- [`ImportedContact$to_list()`](#method-ImportedContact-to_list)

- [`ImportedContact$bytes()`](#method-ImportedContact-bytes)

- [`ImportedContact$clone()`](#method-ImportedContact-clone)

------------------------------------------------------------------------

### Method `new()`

#### Usage

    ImportedContact$new(user_id, client_id)

------------------------------------------------------------------------

### Method `to_list()`

#### Usage

    ImportedContact$to_list()

------------------------------------------------------------------------

### Method `bytes()`

#### Usage

    ImportedContact$bytes()

------------------------------------------------------------------------

### Method `clone()`

The objects of this class are cloneable with this method.

#### Usage

    ImportedContact$clone(deep = FALSE)

#### Arguments

- `deep`:

  Whether to make a deep clone.
