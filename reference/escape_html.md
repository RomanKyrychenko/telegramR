# Escape HTML special characters.

Replaces ampersands, angle brackets, and double quotes with their
corresponding HTML entities.

## Usage

``` r
escape_html(x)
```

## Arguments

- x:

  A character vector containing text to escape.

## Value

A character vector with HTML-sensitive characters converted to their
entity equivalents.
