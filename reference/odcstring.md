# Converts a raw vector to an odd-length string.

Converts a raw vector to an odd-length string.

## Usage

``` r
odcstring(raw_data)
```

## Arguments

- raw_data:

  The raw vector to convert.

## Value

A string with odd-length characters.
