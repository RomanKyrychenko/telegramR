# ReqPqMultiRequest

Telegram API type ReqPqMultiRequest

Methods: - new(nonce): create new instance. - to_list(): return an R
list representation. - to_raw(): serialize to raw vector (bytes) in
little endian as used in TL.

Note: 128-bit integer handling is represented here as numeric/double
placeholders. For precise 128-bit two's-complement behavior, use a
big-integer library and implement a precise serializer.

Represents a request for the server's PQ (prime factor) during the
initial key exchange (multi variant). Holds a 128-bit \`nonce\`
generated by the client.

## Details

ReqPqMultiRequest Class

Represents the TL request \`ReqPqMultiRequest\`.

## Public fields

- `CONSTRUCTOR_ID`:

  A unique identifier for the TL object.

- `SUBCLASS_OF_ID`:

  A unique identifier for the subclass of the TL object.

- `nonce`:

  The \`nonce\` value as an integer.

## Methods

### Public methods

- [`ReqPqMultiRequest$new()`](#method-ReqPqMultiRequest-new)

- [`ReqPqMultiRequest$to_list()`](#method-ReqPqMultiRequest-to_list)

- [`ReqPqMultiRequest$to_bytes()`](#method-ReqPqMultiRequest-to_bytes)

- [`ReqPqMultiRequest$clone()`](#method-ReqPqMultiRequest-clone)

------------------------------------------------------------------------

### Method `new()`

#### Usage

    ReqPqMultiRequest$new(nonce)

------------------------------------------------------------------------

### Method `to_list()`

#### Usage

    ReqPqMultiRequest$to_list()

------------------------------------------------------------------------

### Method `to_bytes()`

#### Usage

    ReqPqMultiRequest$to_bytes()

------------------------------------------------------------------------

### Method `clone()`

The objects of this class are cloneable with this method.

#### Usage

    ReqPqMultiRequest$clone(deep = FALSE)

#### Arguments

- `deep`:

  Whether to make a deep clone.

## Public fields

- `nonce`:

  Field. Initialize a ReqPqMultiRequest

- `class`:

  Field.

## Active bindings

- `class`:

  Field.

## Methods

### Public methods

- [`ReqPqMultiRequest$new()`](#method-ReqPqMultiRequest-new)

- [`ReqPqMultiRequest$to_list()`](#method-ReqPqMultiRequest-to_list)

- [`ReqPqMultiRequest$to_bytes()`](#method-ReqPqMultiRequest-to_bytes)

- [`ReqPqMultiRequest$clone()`](#method-ReqPqMultiRequest-clone)

------------------------------------------------------------------------

### Method `new()`

#### Usage

    ReqPqMultiRequest$new(nonce)

------------------------------------------------------------------------

### Method `to_list()`

#### Usage

    ReqPqMultiRequest$to_list()

------------------------------------------------------------------------

### Method `to_bytes()`

#### Usage

    ReqPqMultiRequest$to_bytes()

------------------------------------------------------------------------

### Method `clone()`

The objects of this class are cloneable with this method.

#### Usage

    ReqPqMultiRequest$clone(deep = FALSE)

#### Arguments

- `deep`:

  Whether to make a deep clone.

## Public fields

- `CONSTRUCTOR_ID`:

  Constructor identifier for this TL object.

- `SUBCLASS_OF_ID`:

  Subclass identifier for this TL object.

- `nonce`:

  Field. Initialize a new ReqPqMultiRequest

- `class`:

  Field.

## Active bindings

- `class`:

  Field.

## Methods

### Public methods

- [`ReqPqMultiRequest$new()`](#method-ReqPqMultiRequest-new)

- [`ReqPqMultiRequest$to_list()`](#method-ReqPqMultiRequest-to_list)

- [`ReqPqMultiRequest$to_bytes()`](#method-ReqPqMultiRequest-to_bytes)

- [`ReqPqMultiRequest$clone()`](#method-ReqPqMultiRequest-clone)

------------------------------------------------------------------------

### Method `new()`

#### Usage

    ReqPqMultiRequest$new(nonce)

#### Arguments

- `nonce`:

  128-bit nonce generated by the client (large int or raw). Convert to
  list representation

  Returns a plain R list useful for debugging or JSON-like inspection.

------------------------------------------------------------------------

### Method `to_list()`

#### Usage

    ReqPqMultiRequest$to_list()

#### Returns

Named list with keys: `"_"` and `nonce`. Serialize to raw bytes

Serializes the request according to the protocol: - constructor id (4
bytes, little-endian) - \`nonce\` as a 128-bit (16-byte) little-endian
value

------------------------------------------------------------------------

### Method `to_bytes()`

#### Usage

    ReqPqMultiRequest$to_bytes()

#### Returns

Raw vector containing the serialized request bytes.

------------------------------------------------------------------------

### Method `clone()`

The objects of this class are cloneable with this method.

#### Usage

    ReqPqMultiRequest$clone(deep = FALSE)

#### Arguments

- `deep`:

  Whether to make a deep clone.
