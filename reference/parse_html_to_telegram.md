# Parse HTML into plain text and telegram-style entities

Parse HTML into plain text and telegram-style entities

## Usage

``` r
parse_html_to_telegram(html)
```

## Arguments

- html:

  character(1) HTML fragment

## Value

named list with elements `text` and `entities`
