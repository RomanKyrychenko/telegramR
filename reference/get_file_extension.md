# Get File Extension

Gets the extension for the given file, which can be either a string or
an open file (which has a `name` attribute).

## Usage

``` r
get_file_extension(file)
```

## Arguments

- file:

  The file path or object.

## Value

The file extension as a string.
