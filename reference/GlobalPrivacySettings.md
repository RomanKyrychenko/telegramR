# GlobalPrivacySettings

Telegram API type GlobalPrivacySettings

## Public fields

- `archive_and_mute_new_noncontactpeers`:

  Field.

- `keep_archived_unmuted`:

  Field.

- `keep_archived_folders`:

  Field.

- `hide_read_marks`:

  Field.

- `new_noncontactpeers_require_premium`:

  Field.

- `display_gifts_button`:

  Field.

- `noncontactpeers_paid_stars`:

  Field.

- `disallowed_gifts`:

  Field.

## Methods

### Public methods

- [`GlobalPrivacySettings$new()`](#method-GlobalPrivacySettings-new)

- [`GlobalPrivacySettings$to_list()`](#method-GlobalPrivacySettings-to_list)

- [`GlobalPrivacySettings$clone()`](#method-GlobalPrivacySettings-clone)

------------------------------------------------------------------------

### Method `new()`

#### Usage

    GlobalPrivacySettings$new(
      archive_and_mute_new_noncontactpeers = NULL,
      keep_archived_unmuted = NULL,
      keep_archived_folders = NULL,
      hide_read_marks = NULL,
      new_noncontactpeers_require_premium = NULL,
      display_gifts_button = NULL,
      noncontactpeers_paid_stars = NULL,
      disallowed_gifts = NULL
    )

------------------------------------------------------------------------

### Method `to_list()`

#### Usage

    GlobalPrivacySettings$to_list()

------------------------------------------------------------------------

### Method `clone()`

The objects of this class are cloneable with this method.

#### Usage

    GlobalPrivacySettings$clone(deep = FALSE)

#### Arguments

- `deep`:

  Whether to make a deep clone.
