# Get Message ID

Similar to `get_input_peer`, but for message IDs.

## Usage

``` r
get_message_id(message)
```

## Arguments

- message:

  The message object or ID.

## Value

The message ID as an integer, or NULL if invalid.
