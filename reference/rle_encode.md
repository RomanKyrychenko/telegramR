# Encode Data with Run-Length Encoding

Encodes a raw vector using run-length encoding.

## Usage

``` r
rle_encode(string)
```

## Arguments

- string:

  A raw vector to encode.

## Value

A raw vector of the encoded data.
