# Guess Extension

Guesses a file extension from a MIME type.

## Usage

``` r
guess_extension(mime_type)
```

## Arguments

- mime_type:

  Character string of the MIME type.

## Value

A character string with the file extension (including dot), or NULL.
