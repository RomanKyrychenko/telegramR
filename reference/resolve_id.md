# Resolve Marked ID

Given a marked ID, returns the original ID and its Peer type.

## Usage

``` r
resolve_id(marked_id)
```

## Arguments

- marked_id:

  An integer representing the marked ID.

## Value

A list containing the original ID and the Peer type (e.g., PeerUser,
PeerChat, or PeerChannel).
