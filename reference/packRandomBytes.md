# Pack random bytes into a raw vector

Pack random bytes into a raw vector

## Usage

``` r
packRandomBytes(n)
```

## Arguments

- n:

  Number of random bytes to generate

## Value

Raw vector of random bytes
