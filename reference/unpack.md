# Unpack data from binary format

Mimics Python's struct.unpack.

## Usage

``` r
unpack(format, data)
```

## Arguments

- format:

  A character string specifying the format.

- data:

  A raw vector of data to unpack.

## Value

A list of unpacked values.
