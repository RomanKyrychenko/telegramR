# Entity Type

Returns EntityType\$USER, \$CHAT, or \$CHANNEL for known
entity/peer/input types.

## Usage

``` r
entity_type(entity)
```

## Arguments

- entity:

  Telegram entity or peer/input peer object.

## Value

One of EntityType\$USER, EntityType\$CHAT, EntityType\$CHANNEL.
