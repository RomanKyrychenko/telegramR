# Unparse plain text and entities back to HTML

Unparse plain text and entities back to HTML

## Usage

``` r
unparse_telegram_to_html(text, entities)
```

## Arguments

- text:

  character(1) plain text

- entities:

  list of entity lists as produced by `parse_html_to_telegram`

## Value

character(1) HTML string
