% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mtprotosender.R, R/utils.R
\name{is_list_like}
\alias{is_list_like}
\title{Check if an object is list-like (list, vector, etc.)}
\usage{
is_list_like(obj)

is_list_like(obj)
}
\arguments{
\item{obj}{The object to check.}

\item{allow_data_frames}{Whether to consider data frames as list-like (default: FALSE)}
}
\value{
TRUE if the object is list-like, FALSE otherwise

A logical value indicating whether the object is list-like.
}
\description{
Determines whether an object can be treated like a list or collection
in the context of MTProto requests.

Returns `TRUE` if the given object looks like a list.
This function checks for common list-like objects in R, such as lists, vectors,
matrices, arrays, data frames, and environments, while excluding strings and raw vectors
to avoid false positives (similar to Python's check ignoring str/bytes).
}
\examples{
\dontrun{
\dontrun{
is_list_like(list(1, 2, 3)) # TRUE
is_list_like(1:10) # TRUE
is_list_like("string") # FALSE
is_list_like(as.raw(c(1, 2))) # FALSE
}
}
}
